# ДОКУМЕНТАЦИЯ СИСТЕМЫ МОНИТОРИНГА НЕФТЕПРОВОДА MonOilStudy v2.0

## ОГЛАВЛЕНИЕ
1. [Общее описание системы](#общее-описание-системы)
2. [Архитектура системы](#архитектура-системы)
3. [Модули системы](#модули-системы)
4. [База данных](#база-данных)
5. [Установка и запуск](#установка-и-запуск)
6. [Руководство пользователя](#руководство-пользователя)
7. [Руководство разработчика](#руководство-разработчика)
8. [Устранение неполадок](#устранение-неполадок)

---

## ОБЩЕЕ ОПИСАНИЕ СИСТЕМЫ

### Назначение
MonOilStudy v2.0 - это комплексная система мониторинга и управления нефтепроводом, разработанная для обеспечения безопасной и эффективной транспортировки нефти. Система предоставляет операторам инструменты для:

- **Мониторинга состояния трубопровода** в режиме реального времени
- **Управления технологическими процессами** перекачки нефти
- **Контроля работы оборудования** и систем безопасности
- **Генерации отчетов** и аналитических данных
- **Управления аварийными ситуациями** и ремонтными работами

### Основные возможности
- ✅ **Визуализация 40 ячеек нефтепровода** с цветовой индикацией состояния
- ✅ **Мониторинг датчиков** давления, температуры, скорости перекачки
- ✅ **Система аутентификации** пользователей с разными уровнями доступа
- ✅ **Управление базами данных** SQLite с возможностью создания и подключения
- ✅ **Генерация отчетов** с экспортом в текстовые файлы
- ✅ **Панель управления** трубопроводом с регулировкой параметров
- ✅ **Диаграмма соединений** для визуализации структуры системы
- ✅ **Управление аварийными бригадами** и ремонтными работами
- ✅ **Автоматическое обновление данных** с настраиваемыми интервалами

### Технические характеристики
- **Язык программирования**: Python 3.7+
- **GUI фреймворк**: Tkinter (встроенный в Python)
- **База данных**: SQLite 3
- **Операционные системы**: Windows, Linux, macOS
- **Архитектура**: Модульная система с 9 независимыми модулями
- **Интерфейс**: Графический пользовательский интерфейс (GUI)

---

## АРХИТЕКТУРА СИСТЕМЫ

### Общая структура
Система построена по модульной архитектуре, где каждый модуль отвечает за определенную функциональность:

```
MonOilStudy v2.0
├── Главное приложение (MonOilStudy_refactored.py)
├── Менеджер баз данных (DatabaseManager)
├── Модули системы (module1.py - module9.py)
├── База данных (new_database.db)
├── Папка отчетов (reports/)
└── Программа управления БД (DB man_0.1.py)
```

### Компоненты системы

#### 1. Главное приложение
**Файл**: `MonOilStudy_refactored.py`
- Создает главное окно с сеткой модулей 3x3
- Управляет подключениями к базе данных
- Обеспечивает аутентификацию пользователей
- Координирует работу между модулями

#### 2. Менеджер баз данных (DatabaseManager)
- Централизованное управление соединениями с БД
- Валидация структуры базы данных
- Безопасное подключение и отключение
- Проверка целостности данных

#### 3. Модульная система
Каждый модуль наследуется от `tk.Frame` и интегрируется в главное окно:
- **Независимость**: модули могут работать автономно
- **Взаимодействие**: модули обмениваются данными через главное приложение
- **Расширяемость**: легко добавлять новые модули

---

## МОДУЛИ СИСТЕМЫ

### Модуль 1: Просмотр данных БД и управление таблицами
**Файл**: `module1.py`

**Назначение**: Визуализация состояния 40 ячеек нефтепровода

**Основные функции**:
- Отображение сетки ячеек 8x5 с цветовой индикацией
- Подключение к различным базам данных
- Выбор таблиц для отображения данных
- Автоматическое обновление состояния ячеек

**Цветовая схема ячеек**:
- 🟢 **Зеленый**: нормальная работа (давление 40-60 МПа)
- 🟡 **Желтый**: предупреждение (давление 30-40 или 60-70 МПа)
- 🔴 **Красный**: критическое состояние (давление <30 или >70 МПа)
- 🔵 **Синий**: ячейка отключена или в обслуживании
- ⚫ **Серый**: нет данных

**Элементы управления**:
- Кнопка "Выбрать таблицу"
- Кнопка "Обновить"
- Переключатель автообновления

### Модуль 2: Мониторинг датчиков в реальном времени
**Файл**: `module2.py`

**Назначение**: Отображение данных датчиков в режиме реального времени

**Типы датчиков**:
1. **Основные датчики** (таблица raw_data):
   - Давление в трубе (МПа)
   - Температура нефти (°C)
   - Скорость перекачки (м/с)
   - Вибрации трубы (мм/с)
   - Угол наклона (град)
   - Температура воздуха (°C)
   - Атмосферное давление (кПа)
   - Скорость ветра (м/с)
   - Влажность воздуха (%)

2. **Внешние датчики** (таблица external_data):
   - Температура среды (°C)
   - Давление среды (кПа)
   - Ветер (м/с)
   - Влажность (%)

**Особенности**:
- Обновление данных каждые 200мс (5 раз в секунду)
- Визуальное выделение изменившихся значений
- Переключение между типами датчиков
- Автоматическое форматирование значений

### Модуль 3: Генерация отчетов и аналитика
**Файл**: `module3.py`

**Назначение**: Создание и управление отчетами о работе системы

**Функции**:
- Генерация отчетов по различным периодам
- Анализ данных трубопровода
- Статистика работы ячеек
- Экспорт отчетов в текстовые файлы
- Сохранение в папку `reports/`

### Модуль 4: Аутентификация и управление пользователями
**Файл**: `module4.py`

**Назначение**: Обеспечение безопасности и управления доступом

**Функции**:
- Вход пользователей в систему
- Проверка учетных данных в БД
- Управление уровнями доступа
- Блокировка/разблокировка модулей
- Смена базы данных

**Таблица пользователей**:
```sql
CREATE TABLE operators (
    operator_id INTEGER PRIMARY KEY,
    operator_fio TEXT,
    operator_login TEXT,
    operator_password TEXT,
    operator_high_priority INTEGER
);
```

### Модуль 5: Панель управления трубопроводом
**Файл**: `module5.py`

**Назначение**: Управление технологическими параметрами

**Параметры управления**:
- **Давление**: 40-60 МПа (регулируемый диапазон)
- **Скорость перекачки**: 3-7 м/с
- **Температура**: 15-25°C

**Управление линиями**:
- 4 линии трубопровода
- 10 ячеек на каждой линии (всего 40 ячеек)
- Индивидуальное управление каждой линией
- Аварийные остановки

### Модуль 6: Настройки системы
**Файл**: `module6.py`

**Назначение**: Конфигурация и настройка системы

**Функции**:
- Настройки интерфейса
- Конфигурация обновлений
- Параметры безопасности
- Настройки уведомлений

### Модуль 7: Диаграмма соединений
**Файл**: `module7.py`

**Назначение**: Визуализация структуры и связей системы

**Функции**:
- Схема трубопровода
- Диаграмма соединений модулей
- Топология сети
- Интерактивная навигация

### Модуль 8: Управление аварийными бригадами
**Файл**: `module8.py`

**Назначение**: Координация аварийно-ремонтных работ

**Функции**:
- Создание заявок на ремонт
- Назначение бригад
- Отслеживание статуса работ
- Журнал аварийных ситуаций
- Связь с модулем 9 для координации

### Модуль 9: Управление базами данных
**Файл**: `module9.py`

**Назначение**: Управление подключениями и настройками БД

**Функции**:
- Подключение к базам данных
- Переключение между БД
- Мониторинг состояния соединения
- Уведомление других модулей о смене БД
- Интеграция с модулем 8

---

## БАЗА ДАННЫХ

### Структура базы данных
Система использует SQLite базу данных со следующими таблицами:

#### Таблица operators (Операторы)
```sql
CREATE TABLE operators (
    operator_id INTEGER PRIMARY KEY AUTOINCREMENT,
    operator_fio TEXT NOT NULL,
    operator_login TEXT UNIQUE NOT NULL,
    operator_password TEXT NOT NULL,
    operator_high_priority INTEGER DEFAULT 0
);
```

#### Таблица cells (Ячейки трубопровода)
```sql
CREATE TABLE cells (
    id INTEGER PRIMARY KEY,
    pressure REAL,
    temperature REAL,
    pumping_speed REAL,
    vibrations REAL,
    tilt_angle REAL,
    working_status TEXT,
    line_number INTEGER,
    cell_number_in_line INTEGER
);
```

#### Таблица raw_data (Основные датчики)
```sql
CREATE TABLE raw_data (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    cell_pressure REAL,
    cell_temperature REAL,
    cell_pumping_speed REAL,
    cell_vibrations REAL,
    cell_tilt_angle REAL,
    outdoor_temperature REAL,
    outdoor_pressure REAL,
    outdoor_wind REAL,
    outdoor_humidity REAL,
    timestamp TEXT
);
```

#### Таблица external_data (Внешние датчики)
```sql
CREATE TABLE external_data (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    env_temperature REAL,
    env_pressure REAL,
    env_wind REAL,
    env_humidity REAL,
    timestamp TEXT
);
```

#### Таблица reports (Отчеты)
```sql
CREATE TABLE reports (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    report_title TEXT,
    report_content TEXT,
    created_at TEXT,
    operator_id INTEGER,
    FOREIGN KEY (operator_id) REFERENCES operators(operator_id)
);
```

### Программа управления БД
**Файл**: `DB man_0.1.py`

**Назначение**: Независимая утилита для управления базами данных

**Функции**:
- Создание новых баз данных с возможностью указания имени
- Подключение к существующим БД
- Просмотр структуры и данных таблиц
- Генерация тестовых данных
- Нормализация данных между серверной и клиентской БД
- Автоматическое обновление данных ячеек

**Возможности**:
- Создание БД с пользовательским именем
- Автоматическое создание всех необходимых таблиц
- Заполнение тестовыми данными
- Экспорт инструкций по подключению

---

## УСТАНОВКА И ЗАПУСК

### Системные требования
- **Python**: версия 3.7 или выше
- **Операционная система**: Windows 10/11, Linux, macOS
- **Оперативная память**: минимум 512 МБ
- **Свободное место на диске**: 100 МБ

### Установка
1. **Убедитесь, что Python установлен**:
   ```bash
   python --version
   ```

2. **Скачайте или клонируйте проект** в папку на вашем компьютере

3. **Проверьте наличие всех файлов**:
   ```
   MonOilStudy_refactored.py  # Главный файл
   module1.py - module9.py    # Файлы модулей
   DB man_0.1.py             # Программа управления БД
   new_database.db           # База данных (создается автоматически)
   ```

### Запуск системы

#### Основная программа
```bash
python MonOilStudy_refactored.py
```
или
```bash
python "MonOilStudy test.py"  # Оригинальная версия
```

#### Программа управления БД
```bash
python "DB man_0.1.py"
```

### Первый запуск
1. **Запустите основную программу**
2. **Система автоматически найдет БД** `new_database.db` или предложит создать новую
3. **Войдите в систему** через модуль 4 (по умолчанию все модули заблокированы)
4. **Начните работу** с разблокированными модулями

---

## РУКОВОДСТВО ПОЛЬЗОВАТЕЛЯ

### Вход в систему
1. **Запустите программу** - откроется окно с сеткой модулей 3x3
2. **Все модули заблокированы** кроме модуля 4 (нижний левый)
3. **Кликните на модуль 4** для входа в систему
4. **Введите логин и пароль** (см. раздел "Учетные данные")
5. **После успешного входа** модули разблокируются

### Учетные данные по умолчанию
Если в БД есть операторы, используйте их данные. Иначе создайте пользователя через программу управления БД.

### Работа с модулями

#### Модуль 1: Просмотр ячеек
1. **Выберите таблицу** кнопкой "Выбрать таблицу"
2. **Наблюдайте за цветами ячеек**:
   - Зеленый = норма
   - Желтый = предупреждение
   - Красный = авария
3. **Используйте автообновление** для мониторинга в реальном времени

#### Модуль 2: Мониторинг датчиков
1. **Переключайтесь между типами датчиков** кнопками вверху
2. **Наблюдайте за данными** в реальном времени
3. **Изменившиеся значения выделяются желтым**
4. **Контролируйте автообновление** кнопкой "Авто"

#### Модуль 3: Отчеты
1. **Генерируйте отчеты** по различным параметрам
2. **Просматривайте статистику** работы системы
3. **Сохраняйте отчеты** в папку reports/

#### Модуль 5: Управление
1. **Регулируйте параметры** трубопровода
2. **Контролируйте линии** и ячейки
3. **Выполняйте аварийные остановки** при необходимости

### Управление базами данных
1. **Используйте модуль 9** для переключения БД
2. **Создавайте новые БД** через программу `DB man_0.1.py`
3. **Мониторьте состояние соединения** по индикаторам

### Генерация отчетов
1. **Откройте модуль 3**
2. **Выберите параметры отчета**
3. **Нажмите "Создать отчет"**
4. **Файл сохранится** в папку reports/ с временной меткой

---

## РУКОВОДСТВО РАЗРАБОТЧИКА

### Архитектура кода

#### Главное приложение
```python
class MonOilStudyApp:
    def __init__(self):
        self.root = tk.Tk()
        self.db_manager = DatabaseManager()
        self.module_tiles = {}
        # Инициализация системы
```

#### Структура модуля
```python
class ModuleX(tk.Frame):
    def __init__(self, parent, app=None):
        super().__init__(parent, bg="white")
        self.app = app
        self.setup_ui()
        
    def setup_ui(self):
        # Создание интерфейса
        
    def on_close(self):
        # Очистка ресурсов
```

### Взаимодействие модулей
Модули взаимодействуют через главное приложение:

```python
# Получение соединения с БД
conn = self.app.get_db_connection()

# Получение ссылки на другой модуль
module5 = self.app.get_module_instance(5)
```

### Добавление нового модуля

1. **Создайте файл** `moduleX.py`:
```python
import tkinter as tk

class ModuleX(tk.Frame):
    def __init__(self, parent, app=None):
        super().__init__(parent, bg="white")
        self.app = app
        # Ваш код
```

2. **Зарегистрируйте в главном приложении**:
```python
# В файле MonOilStudy_refactored.py
from moduleX import ModuleX

# В методе _initialize_module класса ModuleTile
elif self.module_number == X:
    self.module_instance = ModuleX(self.content_frame, self.app)
```

### Работа с базой данных

#### Получение соединения
```python
def get_connection(self):
    if self.app and hasattr(self.app, 'get_db_connection'):
        return self.app.get_db_connection()
    return None
```

#### Выполнение запросов
```python
try:
    conn = self.get_connection()
    cursor = conn.cursor()
    cursor.execute("SELECT * FROM table_name")
    results = cursor.fetchall()
except sqlite3.Error as e:
    print(f"Ошибка БД: {e}")
```

### Обработка ошибок
Все модули должны обрабатывать ошибки:

```python
try:
    # Ваш код
except sqlite3.Error as e:
    print(f"Ошибка БД: {e}")
except Exception as e:
    print(f"Системная ошибка: {e}")
```

### Очистка ресурсов
Каждый модуль должен иметь метод `on_close()`:

```python
def on_close(self):
    # Отменяем таймеры
    if self.update_task:
        self.after_cancel(self.update_task)
    
    # Закрываем соединения
    if self.local_conn:
        self.local_conn.close()
```

---

## УСТРАНЕНИЕ НЕПОЛАДОК

### Частые проблемы и решения

#### 1. Программа не запускается
**Проблема**: Ошибка импорта модулей
**Решение**:
- Убедитесь, что все файлы модулей находятся в одной папке
- Проверьте версию Python (требуется 3.7+)
- Проверьте наличие всех файлов: module1.py - module9.py

#### 2. Нет подключения к БД
**Проблема**: Модули показывают "Нет подключения к БД"
**Решение**:
- Проверьте наличие файла `new_database.db`
- Создайте новую БД через `DB man_0.1.py`
- Используйте модуль 9 для подключения к БД

#### 3. Модули заблокированы
**Проблема**: Все модули кроме 4 показывают замок
**Решение**:
- Войдите через модуль 4 (аутентификация)
- Проверьте наличие пользователей в таблице operators
- Создайте пользователя через программу управления БД

#### 4. Ошибки синтаксиса
**Проблема**: SyntaxError при запуске
**Решение**:
- Проверьте целостность файлов модулей
- Убедитесь, что файлы не повреждены
- Скачайте файлы заново

#### 5. Медленная работа
**Проблема**: Система работает медленно
**Решение**:
- Отключите автообновление в модулях 1 и 2
- Увеличьте интервал обновления
- Проверьте размер базы данных

### Логи и отладка
- Все модули выводят отладочную информацию в консоль
- Ошибки отображаются с префиксом модуля (например, "Module1: ОШИБКА")
- Используйте программу управления БД для проверки данных

### Восстановление системы
1. **Создайте новую БД** через `DB man_0.1.py`
2. **Перезапустите программу**
3. **Войдите с новыми учетными данными**
4. **Восстановите настройки** через модуль 6

### Контакты поддержки
При возникновении проблем:
1. Проверьте консольный вывод на наличие ошибок
2. Убедитесь в целостности файлов БД
3. Попробуйте создать новую БД
4. Обратитесь к разработчикам с подробным описанием проблемы

---

## ЗАКЛЮЧЕНИЕ

Система MonOilStudy v2.0 представляет собой мощный инструмент для мониторинга и управления нефтепроводом. Модульная архитектура обеспечивает гибкость и расширяемость системы, а подробная документация и комментарии в коде облегчают сопровождение и развитие проекта.

Система успешно решает задачи:
- ✅ Визуального мониторинга состояния трубопровода
- ✅ Управления технологическими параметрами
- ✅ Обеспечения безопасности и контроля доступа
- ✅ Генерации отчетности и аналитики
- ✅ Координации аварийно-ремонтных работ

**Версия документации**: 2.0  
**Дата создания**: 18 июня 2025  
**Статус**: Финальная версия рефакторированной системы 